# Create a method in class with name notRegSub (Decide whether it should be instance method, a class method or static method).

class Student:
    def __init__(self, name):
        self.name = name
        self.reg_subs = []
         